"""Golden-snapshot contract tests pinning current HTTP behavior.

Covers the proxy and management services, plus the shared
snapshot/subprocess harness they run against (see `conftest.py` and
`snapshot.py`).
"""
